#!/usr/bin/env bash

test -d reports/log || mkdir -p reports/log

git log --format=%cn $@ | sort | uniq -c | sort -rn | tee reports/log/git_top_committers.log

# todo: transform the git_top_committers.log to JSON and save in a ./reports directory.
